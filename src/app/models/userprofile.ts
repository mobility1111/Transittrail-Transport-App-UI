
export interface UserProfile {
    userId: string; // Assuming a unique identifier for the user
    userName: string;
    firstName: string;
    lastName: string;
    email: string;
    // Add more properties as needed
  }
  